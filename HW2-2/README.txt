The codes in professor's example, hw.xml, shows that the jar files have prefixs "org", but I downloaded them from 
IntellJ(IDE) without "org", whick looks like "junit-jupiter-5.4.2.jar", "junit-platform-commons-1.4.2.jar" and so on.
Thus I modified their name to match the codes needed.